# TEMPORARY — see robots.txt's own header comment and
# red-door-website-todo.md's "Pre-launch: remove the temporary noindex"
# item. X-Robots-Tag is the authoritative signal (works even for crawlers
# that fetch pages despite robots.txt, and for non-HTML responses);
# robots.txt is defense in depth on top of it. MUST BE REMOVED before
# this deploys to the real reddoorrents.com domain.
/*
  X-Robots-Tag: noindex, nofollow
